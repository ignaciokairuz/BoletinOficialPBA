name: Actualización Diaria

on:
  schedule:
    # 10:00 AM Argentina (13:00 UTC) - después de publicación del boletín
    - cron: '0 13 * * 1-5'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  update_site:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scraper
        run: python scraper/scrape_boletin.py

      - name: Push changes
        run: |
          git config --global user.name 'Bot Boletín PBA'
          git config --global user.email 'bot@noreply.github.com'
          
          git add docs/
          git diff --staged --quiet || git commit -m "📊 Actualización $(date +'%Y-%m-%d %H:%M')"
          git push
